# Stripe — get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Price IDs — create products/prices in the Stripe Dashboard, then paste IDs here
STRIPE_PRICE_MONTHLY=price_...
STRIPE_PRICE_QUARTERLY=price_...
STRIPE_PRICE_ANNUAL=price_...
STRIPE_PRICE_GIFT=price_...

# Stripe Webhook Secret — from `stripe listen --forward-to localhost:3001/api/payments/webhook`
STRIPE_WEBHOOK_SECRET=whsec_...

# Sentry — error tracking (https://sentry.io → Project Settings → Client Keys)
SENTRY_DSN=https://...@....ingest.sentry.io/...
VITE_SENTRY_DSN=https://...@....ingest.sentry.io/...

# Sentry — source map uploads (https://sentry.io → Settings → Auth Tokens)
# Only needed in CI / build environments
SENTRY_AUTH_TOKEN=sntrys_...
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug
